{
  "version": 2,
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ],
  "builds": [
    { "src": "api/index.js", "use": "@vercel/node", "config": { "maxDuration": 30 } }
  ],
  "installCommand": "npm install --omit=dev --prefer-offline --no-audit --no-fund"
}