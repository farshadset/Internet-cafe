{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node", "excludeFiles": ["public/**"] }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/server.js" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}